using UnityEngine;
using System.Collections;
using ModuleIsolation;
using killer;
using Killer.Game.Server;
using killer.proto;
using killer.common;
using dataconfig;
using Killer.DataConfig;

namespace Killer.Game.{0}
{
    public class {0}CFGParser : ModuleIsolationBase
    {
        
        private static {0}CFGParser m_instance;
        public static void Start( {0}CFGParser proxy )
        {
            if (null == m_instance)
            {
                m_instance = proxy;
            }
        }
        
        public static {0}CFGParser GetInstance()
        {
            if (null == m_instance)
            {
                #if LOG_DETAIL
                Debug.LogError("has no Instance '{0}CFGParser'");
                #endif
                throw new UnityException("has no Instance '{0}CFGParser'");
            }
            return m_instance;
        }

        public {0}CFGParser( ModulePostman postman ) : base( postman )
        {
        }
    }
}